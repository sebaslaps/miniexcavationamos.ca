{
  "name": "miniexcavationamos-ca",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "astro build",
    "dev": "astro dev",
    "local": "astro dev --host 127.0.0.1 --port 4321",
    "preview": "astro preview --host 127.0.0.1 --port 4321"
  },
  "dependencies": {
    "astro": "latest"
  },
  "devDependencies": {}
}
